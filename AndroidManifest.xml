<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twofours.surespot"
    android:versionCode="2"
    android:versionName="0.1" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <permission
        android:name="com.twofours.surespot.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.twofours.surespot.permission.C2D_MESSAGE" />
    <!-- App receives GCM messages. -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <!-- GCM connects to Google Services. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- GCM requires a Google account. -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- Keeps the processor from sleeping when a message is received. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name="com.twofours.surespot.SurespotApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="surespot"
        android:theme="@style/Theme.Sherlock.Light.DarkActionBar" >
        <receiver
            android:name="com.google.android.gcm.GCMBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="com.twofours.surespot" />
            </intent-filter>
        </receiver>

        <service android:name="com.twofours.surespot.GCMIntentService" />
        <service
            android:name="com.twofours.surespot.services.CredentialCachingService"
            android:exported="false" >
        </service>

        <activity
            android:name="com.twofours.surespot.activities.StartupActivity"
            android:label="@string/title_activity_main"
            android:theme="@android:style/Theme.NoDisplay" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.SEND_MULTIPLE" /> -->
            <!-- <category android:name="android.intent.category.DEFAULT" /> -->
            <!-- <data android:mimeType="image/*" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name="com.twofours.surespot.activities.SignupActivity"
            android:label="@string/title_activity_signup" >
            <intent-filter>
                <action android:name="com.twofours.surespot.activities.SignupActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.twofours.surespot.activities.LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.twofours.surespot.friends.FriendActivity"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="com.twofours.surespot.chat.ChatActivity"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity android:name="com.twofours.surespot.activities.ImageViewActivity" >
        </activity>
        <activity android:name="com.twofours.surespot.activities.ImageSelectActivity" >
        </activity>
        <activity
            android:name="com.twofours.surespot.activities.SettingsActivity"
            android:parentActivityName="com.actionbarsherlock.app.SherlockPreferenceActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.actionbarsherlock.app.SherlockPreferenceActivity" />
        </activity>
        <activity android:name="com.twofours.surespot.activities.ExportIdentityActivity" >
            <intent-filter>
                <action android:name="com.twofours.surespot.activities.ExportIdentityActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name="com.twofours.surespot.activities.ImportIdentityActivity" >
            <intent-filter>
                <action android:name="com.twofours.surespot.activities.ImportIdentityActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>